пусть лс ложь,правда,[]

пусть иТаблица
  применить Список
    для а в лс вернуть
      для б в лс вернуть
        строка(а)+" & "+строка(б)+" = "+строка(а & б)
    >>=(от сп сп)
  отобразить(от елем вывод елем)

пусть илиТаблица
  применить Список
    для а в лс вернуть
      для б в лс вернуть
        строка(а)+" | "+строка(б)+" = "+строка(а | б)
    >>=(от сп сп)
  отобразить(от елем вывод елем)

пусть еститоТаблица
  применить Список
    для а в лс вернуть
      для б в лс вернуть
        строка(а)+" -> "+строка(б)+" = "+строка(не а | б)
    >>=(от сп сп)
  отобразить(от елем вывод елем)

применить ВводВывод
    свернуть(>>)начало иТаблица>>
    вывод "---">>
    свернуть(>>)начало илиТаблица>>
    вывод "---">>
    свернуть(>>)начало еститоТаблица

